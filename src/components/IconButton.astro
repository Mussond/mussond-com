---
interface Props {
  label: string;
  href?: string;
  target?: string;
  rel?: string;
  size?: 'default' | 'small';
  type?: 'button' | 'submit' | 'reset';
  class?: string;
  title?: string;
  // Forwarded ARIA attributes for drawer wiring etc.
  'aria-haspopup'?: string;
  'aria-expanded'?: string;
  'aria-controls'?: string;
  'aria-keyshortcuts'?: string;
}

const {
  label,
  href,
  target,
  rel,
  size = 'default',
  type = 'button',
  class: className,
  title,
  'aria-haspopup': ariaHasPopup,
  'aria-expanded': ariaExpanded,
  'aria-controls': ariaControls,
  'aria-keyshortcuts': ariaKeyshortcuts,
} = Astro.props;

const classes = [
  'icon-button',
  size === 'small' ? 'icon-button--small' : '',
  className,
].filter(Boolean).join(' ');
---

{href ? (
  <a
    href={href}
    class={classes}
    aria-label={label}
    target={target}
    rel={rel}
  >
    <slot />
    <span class="icon-button__label">{label}</span>
  </a>
) : (
  <button
    type={type}
    class={classes}
    aria-label={label}
    title={title}
    aria-haspopup={ariaHasPopup}
    aria-expanded={ariaExpanded}
    aria-controls={ariaControls}
    aria-keyshortcuts={ariaKeyshortcuts}
  >
    <slot />
    <span class="icon-button__label">{label}</span>
  </button>
)}
