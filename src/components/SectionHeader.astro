---
interface Props {
  eyebrow: string;
  heading: string;
  headingId: string;
}

const { eyebrow, heading, headingId } = Astro.props;
---

<div class="section-header">
  <p class="section-header__eyebrow">{eyebrow}</p>
  <h2 id={headingId} class="section-header__heading">{heading}</h2>
</div>
